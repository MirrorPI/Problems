#include <iostream>
using namespace std;

int main(){
    ios_base::sync_with_stdio(0);
    cin.tie(0);

    int A, B, result;
    cin>>A>>B;
    result = A + B;

    cout<<result;
    return 0;
}